# Static metadata for this agent.
# Referenced by scripts/create-agent.sh.

name: Python Backend Developer
description: Builds FastAPI services with uv, tests, structured logs, Makefile, and Dockerfile
# Model is passed via custom_args because the Claude-on-Bedrock runtime
# expects the region-qualified ID (global.anthropic.claude-sonnet-4-6).
model: ""
visibility: private
max_concurrent_tasks: 4
