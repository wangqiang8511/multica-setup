# Environment variables passed to the agent runtime (custom_env).
# KEY=VALUE, one per line. Blank lines and `#` comments are ignored.
# Do not put secrets here — this file is checked in.

# Example:
# CLAUDE_CODE_USE_BEDROCK=1
